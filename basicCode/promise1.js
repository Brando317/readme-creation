const p = promise (watchTurorialCallback(callback, errorCallback)) {
let userLeft = false;
Let userWatchingLiveStream = True;

if (userLeft) {
   errorCallback(
       {
           name: "User Left",
           message: ":(")
        }
        else if (userWatchingLiveStream) {
            callback ("ThumbsUp and subscribed!")
        }
        }
       }

       watchTurorialCallback